#include "header.h"
#include <stdio.h>
#include <string.h>
#include <fstream>
#include <iostream>
#include <string>

using namespace std;

// 변수 선언
FILE* in_fp, * out_fp;

void Login::login(string id, string password) {
	// member 함수의 authenticateMember()
}